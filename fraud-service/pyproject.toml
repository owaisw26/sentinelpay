[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "localstack: acceptance tests requiring a running LocalStack instance",
    "postgres: integration tests requiring Docker",
]
