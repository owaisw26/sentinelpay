# Use local personas and the development-only token endpoint by default.
VITE_AUTH_MODE=local

# The AWS deployment supplies these values for the Cognito-backed build.
VITE_COGNITO_AUTHORITY=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_API_AUDIENCE=
VITE_COGNITO_SCOPES=openid profile email
